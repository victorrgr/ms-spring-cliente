spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:./data/proj-cliente
    username: admin
    password: admin
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console